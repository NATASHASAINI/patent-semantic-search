from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def home():
    return {"message": "Patent Semantic Search API running"}

@app.post("/search")
def search(query: dict):
    return {
        "query": query.get("query"),
        "results": ["dummy result 1", "dummy result 2"]
    }
